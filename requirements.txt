# Core analysis — used by the notebook and the src/ package
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
matplotlib>=3.7
seaborn>=0.13

# LLM layer — only needed if you re-run the taxonomy cell
# A cached taxonomy is committed to outputs/taxonomy_cache.json
# so the notebook still runs end-to-end without an API key.
groq>=0.4

# Optional alternative path used in src/llm_pipeline.py (OpenAI SDK)
# openai>=1.0
